export interface UsuarioData {
    accion: number;
    nIdUsuario:number;
}

export interface ListaData {
    valor: number;
    nombre: string;
}

export interface GeneroData {
    abrev: string;
    nombre: string;
}